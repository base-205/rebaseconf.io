import PageContainer from 'components/PageContainer';

const Home = props => {
    return (
      <PageContainer>
        <p>This is the home page.</p>
      </PageContainer>
  );
};

export default Home;
